{
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"name": "jorytindall",
	"scripts": {
		"web:dev": "yarn workspace web start",
		"web:build": "yarn workspace web build",
        "admin:dev": "yarn workspace admin start",
        "admin:build": "yarn workspace admin build",
		"format:all": "prettier --write ."
	},
	"devDependencies": {
		"prettier": "^2.7.1",
		"turbo": "^1.4.2"
	}
}
